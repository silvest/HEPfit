/* 
 * File:   obliqueShat.cpp
 * Author: mishima
 */

#include <cstdlib>
#include <iostream>
#include <iomanip>
#include <cmath>
#include "obliqueShat.h"


obliqueShat::obliqueShat(const EW& EW_i) : ThObservable(EW_i) {
}

double obliqueShat::getThValue() {   
    
    std::cout << "Write codes for obliqueShat::getThValue()!" << std::endl;
    exit(EXIT_FAILURE); 
    
    return (0.0);
}
 

